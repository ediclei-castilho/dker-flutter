version: 2
jobs:
  build:
    machine: true
    
    steps:
      - checkout
      - run:
          name: Test
          command: PROJECT_TEST=odroidfeeder
          command: /bin/bash cloud/docker/build-test.sh
