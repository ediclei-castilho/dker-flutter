version: 2
jobs:
  build:
    machine: true
    
    steps:
      - checkout
      - run:
          name: "Qual a branch!?"
          command: echo ${CIRCLE_BRANCH}
      - run:
          name: "Test"
          command: echo 'export PROJECT_TEST="odroidfeeder"' >> $BASH_ENV
          command: /bin/bash cloud/docker/build-test.sh $PROJECT_TEST
